package org.example;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Main {
    public static void main(String[] args) {

        // below example code
        /*
        // pass root to Tree constructor
        Node root = new Node(10, null, null);
        Node child = new Node(20, null, null);
        root.setLeftChild(child);
        Tree tree0 = new Tree(root);

        System.out.println("Number of nodes without child for tree0: "+tree0.getNumberOfNodesWithoutChild());
        System.out.println("Largest number of edges in a path from the root node to a leaf node for tree0: " + tree0.getMaxDistance());


        // build trees using arrays
        List<Integer> values1 = Arrays.asList(
                5, 3, 7, 2, 5, 1, 0, null, null, null, null,
                null, null, 2, 8, null, null, null, null,
                null, null, null, null, null, null, null,
                null, null, null, null, 5
        );

        List<Integer> values2 = Arrays.asList(
                2, 3, 7, 2, 5, 1, 0, null, null, null, null,
                null, null, 2, 8, null, null, null, null,
                null, null, null, null, null, null, null,
                null, null, null, null, 5
        );

        Tree tree1 = new Tree(values1);
        System.out.println("Number of nodes without child for tree1: "+tree1.getNumberOfNodesWithoutChild());
        System.out.println("Largest number of edges in a path from the root node to a leaf node for tree1: " + tree1.getMaxDistance());

        Tree tree2 = new Tree(values2);
        System.out.println("Number of nodes without child for tree2: "+tree2.getNumberOfNodesWithoutChild());
        System.out.println("Largest number of edges in a path from the root node to a leaf node for tree2: " + tree2.getMaxDistance());

        System.out.println("Check if tree1 is equal to tree2: " + tree1.equal(tree2));
        System.out.println("Check if tree0 is equal to tree1: " + tree1.equal(tree0));
        System.out.println("Check if tree0 is equal to tree2: " + tree0.equal(tree2));
        System.out.println("Check if tree0 is equal to tree0: " + tree0.equal(tree0));
        */
    }
}